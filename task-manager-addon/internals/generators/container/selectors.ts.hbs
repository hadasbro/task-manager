import { createSelector } from '@reduxjs/toolkit';

import { RootStateSla } from 'types';
import { settingsSliceInitialState } from './slice';

const selectDomain = (state: RootStateSla) => state.{{ camelCase ComponentName }} || settingsSliceInitialState;

export const select{{ properCase ComponentName }} = createSelector(
  [selectDomain],
  {{ camelCase ComponentName }}State => {{ camelCase ComponentName }}State,
);
